# Script Video 314 — mcp-use

## Thông tin
- Tool/Repo/Skill liên quan: /repos/mcp-use.md
- Platform: TikTok / YouTube Shorts
- Thời lượng dự kiến: ~45 giây

## Hook (3 giây đầu)
"Nối một AI với năm MCP server cùng lúc, đừng tự viết tay quản lý connection."

## Script voiceover (ElevenLabs-ready)
[Đoạn 1 — vấn đề/pain point]
Muốn một AI dùng được nhiều nguồn dữ liệu MCP khác nhau cùng lúc, bình thường phải tự viết code quản lý kết nối, tự xử lý mất kết nối, tự liệt kê công cụ từng server.

[Đoạn 2 — giới thiệu giải pháp]
Mcp-use gói hết vào một thư viện Python, vừa làm client nối trực tiếp gọi công cụ, vừa làm agent để AI tự suy luận chọn công cụ nào cần dùng qua nhiều bước.

[Đoạn 3 — demo/cách làm]
Khai báo hết các MCP server cần nối trong một file cấu hình duy nhất, gọi client là có ngay danh sách công cụ, bọc thêm agent là AI tự chạy được nhiều bước không cần viết logic điều hướng bằng tay.

[Đoạn 4 — kết + CTA]
Không cần học lại từ đầu nếu đã quen MCP, chỉ là gói gọn phần nối nhiều server lại cho gọn. Link cài đặt và ví dụ mẫu tao để trong kho, comment "mcp use" là gửi.

## Ghi chú quay (OBS)
- Cảnh 1: Nhiều dây cắm lộn xộn nối vào 1 máy — lúc hook/pain point
- Cảnh 2: Sơ đồ 1 file config nối tới nhiều MCP server — lúc giới thiệu
- Cảnh 3: Terminal gõ pip install mcp-use rồi code mẫu MCPClient/MCPAgent — lúc demo
- Cảnh 4: Show logo mcp-use — lúc kết

## Caption/Sub note (CapCut)
Highlight: "nhiều MCP server cùng lúc", "một file cấu hình duy nhất", "AI tự suy luận chọn công cụ". Giữ nhịp gọn vì đây là tool kỹ thuật, không cần kịch tính quá.

## Thumbnail idea (Canva)
1 icon Python ở giữa, nhiều đường dây nối ra các icon MCP server khác nhau, text to: "GỌN NHIỀU MCP SERVER TRONG 1 THƯ VIỆN".

## CTA cuối video
Comment "mcp use" nhận link cài đặt + ví dụ code mẫu nối nhiều MCP server, follow để xem thêm tool cho dev làm agent.
